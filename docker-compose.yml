services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - db-data:/app/instance     # Persistencia de la BD SQLite
    env_file:
      - .env                      # Carga las variables de entorno
    restart: unless-stopped

volumes:
  db-data:                        # Volumen nombrado para la BD
